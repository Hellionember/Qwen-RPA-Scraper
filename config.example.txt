# ==================== АВТОРИЗАЦИЯ ====================
# ВНИМАНИЕ: Логин и пароль теперь хранятся в скрытом файле .env (для безопасности)!
# Не вписывайте их сюда!
# QWEN_EMAIL = 
# QWEN_PASSWORD =

# ==================== ПАРСИНГ ОТВЕТА ====================

# Разделитель, который модель должна использовать в ответе CSV
CSV_SEPARATOR = €

# Кодировка для сохранения файлов
EXPECTED_ENCODING = utf-8

# ==================== ПУТИ И ФАЙЛЫ ====================

# Входной Excel файл с данными (обязателен)
INPUT_EXCEL = Входной.xlsx

# Путь к файлу базы данных SQLite (создаётся автоматически)
DB_PATH = materials.db

# Выходной Excel файл с результатами обработки
OUTPUT_EXCEL = Выходной.xlsx

# Путь к драйверу Edge браузера (нужен для автоматизации)
EDGE_DRIVER_PATH = C:\drivers\msedgedriver.exe

# Файл для сохранения логов (ошибки, предупреждения, информация)
LOG_FILE = processing.log

# Папка для сохранения отладочных файлов при ошибках
DEBUG_DIR = debug

# ==================== ПАРАМЕТРЫ ОБРАБОТКИ ====================

# Сколько записей отправлять за один раз в модель
BATCH_SIZE = 100

# Сколько записей отправлять за один раз в модель при повторной проверке
RETRY_BATCH_SIZE = 50

# Минимальная задержка между батчами (секунды)
DELAY_MIN = 5

# Максимальная задержка между батчами (секунды)
DELAY_MAX = 15

# Через сколько успешных батчей создавать новый диалог в чате
NEW_CHAT_EVERY_N_BATCHES = 10

# Сколько раз повторять попытку, если в ответе нет разделителя €
MAX_RETRIES = 5

# Максимальное число повторных попыток обработки пропущенных номеров
MAX_RETRY_PER_ROW = 5

# Сколько раз повторять попытку при таймауте (будет создан новый чат)
MAX_TIMEOUT_RETRIES = 5

# Максимальное время ожидания ответа от модели (секунды)
RESPONSE_TIMEOUT = 900

# ==================== ОЖИДАНИЯ И ТАЙМАУТЫ ====================

# Задержка после открытия страницы Qwen (секунды)
PAGE_LOAD_DELAY = 15

# Базовое время ожидания появления элементов на странице (секунды)
WAIT_FOR_ELEMENT_TIMEOUT = 15

# Время ожидания при создании нового чата (секунды)
WAIT_FOR_NEW_CHAT_TIMEOUT = 15

# Время ожидания при переключении режима модели (секунды)
SWITCH_MODE_TIMEOUT = 15

# Время ожидания после нажатия кнопки входа (секунды)
LOGIN_TIMEOUT = 15

# ==================== ЗАДЕРЖКИ МЕЖДУ ДЕЙСТВИЯМИ ====================

# Задержка после клика мышью (секунды)
AFTER_CLICK_DELAY = 3

# Задержка после очистки поля ввода (секунды)
AFTER_CLEAR_DELAY = 2

# Задержка после вставки текста (секунды)
AFTER_PASTE_DELAY = 3

# Задержка после нажатия Enter для отправки запроса (секунды)
AFTER_ENTER_DELAY = 8

# Дополнительная задержка после получения ответа от модели (секунды)
EXTRA_RESPONSE_DELAY = 8

# ==================== ПОВЕДЕНИЕ СКРИПТА ====================

# Сохранять ли отладочные файлы при ошибках (True/False)
SAVE_DEBUG_FILES = True

# Автоматически ли создавать новый чат при необходимости (True/False)
AUTO_CREATE_NEW_CHAT = True

# Запускать ли браузер в фоновом режиме (True/False)
HEADLESS_MODE = False

# Ширина окна браузера (пиксели)
WINDOW_WIDTH = 1920

# Высота окна браузера (пиксели)
WINDOW_HEIGHT = 1080

# ==================== ЛОГИРОВАНИЕ ====================

# Уровень детализации логов (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL = DEBUG
